services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: konvoi
      POSTGRES_PASSWORD: konvoi
      POSTGRES_DB: konvoi
    ports:
      - "5433:5432"
    volumes:
      - konvoi_pgdata:/var/lib/postgresql/data

volumes:
  konvoi_pgdata:
