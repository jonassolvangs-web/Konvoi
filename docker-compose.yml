services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: turbo
      POSTGRES_PASSWORD: turbo
      POSTGRES_DB: turbo
    ports:
      - "5433:5432"
    volumes:
      - turbo_pgdata:/var/lib/postgresql/data

volumes:
  turbo_pgdata:
